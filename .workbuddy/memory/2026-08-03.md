# 2026-08-03

## 《从一句提示词到模型上下文》去 AI 味（Humanizer skill）

- 用户要求用 builtin `humanizer` skill 优化文章。判断：技术知识库文章，项目约定「简明、逻辑清晰、去 AI 味」，且一直用 polish skill 维持正式技术文档风格，故**只做去 AI 味模式清除，不加 Humanizer 的「第一人称/情绪/幽默」部分**（会破坏文档语气与 polish 中立性）。
- 改 5 处：①第40行「由这套流程驱动」→「都走这套流程」；②第82行「正是…的组合」→「这就是…」；③第166行「把链路收尾」→「链路走到最后」；④第178行「被放在了正确的位置」→「前面已经接好了 Harness 准备的环境、记忆、历史和工具结果」（去空泛隐喻）；⑤第201行总结「不是雕琢那句话，而是设计…」（Humanizer 模式9 负向平行）→ 正向陈述「真正的难点是设计它前后所有被组合、组装和传递的片段」。
- 保留设计启示的加粗术语名（功能性加粗，非装饰，属 polish 确立版式）。
- 校验：validate_markdown.py passed；git diff --check ok；专项扫描 U+FFFD=0、破折号=0、其他=0。
- 仍未处理（用户未批准）：设计启示「检索内容」→「附加上下文」（范围漂移）；查漏补缺提的 cache_control 呼应、权限模式回扣两处真实缺口（用户只说「查漏」未批准改）。
- 15:58 标题「回看上下文构成」→「回顾上下文构成」（用户指定）；同日按用户要求「去掉一些无意义的加粗」：移除链路总览列表的 **组合/组装/传递** 标签加粗（概念+解释式机械加粗）与第107行嵌句「最后一条 user 消息」强调加粗；保留设计启示 6 个要点短语 + 3 个阶段引导语的要点提炼式加粗（功能性）。校验 passed，U+FFFD/破折号/其他=0。
- 16:02 用户嫌「设计启示」段加粗太多（9 处）、排版不舒服。重排：去掉 6 个要点短语加粗（记忆要克制/稳定前缀前置/提示词是追加不是全部/循环会放大上下文/约束落地代码/不可信数据标记），仅保留 3 个阶段引导语作为小节锚点加粗；列表恢复「标签：解释」普通结构。加粗 9→3 处。校验 passed，U+FFFD=0。

## 新增《CopilotKit 介绍与快速入门》

- 用户要求写 CopilotKit 介绍 + 快速入门。放在 `apps/docs/ai/Agent/CopilotKit 介绍与快速入门.md`，`order: 10`、`draft: true`（新文章默认草稿，本地 dev 可见，prod 构建排除）。
- 内容：定位（agent 的前端栈）、与裸 API 的区别、核心能力、架构数据流（D2 图）、关键概念、Next.js App Router 7 步快速入门（v2 API，BuiltInAgent + CopilotRuntime）、useCopilotReadable/useCopilotAction 让 Agent 操作应用、下一步。
- 遵循约定：长代码块加 `fold`、文件路径写入 `title`、流程用 d2、去 AI 味。
- 未跑 docs:build（AGENTS.md：改文档不自动构建，除非用户要求）。

## 《CopilotKit 介绍与快速入门》去 AI 味（Humanizer skill）

- 用户用 humanizer skill 要求优化。沿用本 KB 既定口径：只做 AI 模式清除，不注入第一人称/情绪/幽默（破坏技术文档中立语气）。
- 改动：① 开头英文 parenthetical 后误用的全角冒号→逗号（"the frontend stack for agents），聊天界面…"）；② 移除 核心能力/关键概念/下一步 三处列表项的机械加粗（共 13 处 **label：**），API 名改靠代码反引号区分；③ 顺手把"分别覆盖…三种产品形态"等略啰嗦表述收紧。
- 校验：grep `\*\*|—` 无残留（无加粗、无破折号）。未跑 build。

## 调整 CopilotKit 文章归属（AI → Agent 实战 分组）

- 用户要求：在 AI 侧边栏新增「Agent 实战」入口，把 CopilotKit 文章放其下，生产不可见（维持 draft: true）。
- 操作：`rtk proxy mv` 把 `apps/docs/ai/Agent/CopilotKit 介绍与快速入门.md` 移到 `apps/docs/ai/Agent实战/`；`rspress.config.ts` 的 `SIDEBAR_GROUP_ORDERS[AI_PATH]` 由 `['Agent']` 改为 `['Agent', 'Agent实战']`（分组顺序与目录名一致，自动生成侧边栏）。
- git：`rtk git add -A` 后旧路径已从 index 移除、新路径已跟踪，移动干净无重复。`draft: true` 保留 → prod 构建排除、dev 显示「（草稿）」。
- 未跑 docs:build（AGENTS.md：改文档不自动构建）。

## 重写《CopilotKit 介绍与快速入门》按三点要求

- 用户要求：①扫盲介绍清晰完整 ②核心能力/核心概念/优势讲清 ③快速入门达到模型对话 MVP。
- 全文重写结构：这是什么（问题/心智模型/D2 架构图/对话链路）→ 核心能力 → 核心概念 → 优势 → 快速入门（6 步，强调 BuiltInAgent 内置、跑通即 MVP）→ 让 Agent 认识应用（readable/action）→ 下一步。
- 去 AI 味保持：无加粗列表标签、无破折号（grep `\*\*|—` 无残留）；概念用 ### 小标题而非加粗标签；示例具体（addTodo / 买牛奶）。
- 仍 `draft: true`、`order: 10`、`apps/docs/ai/Agent实战/`。未跑 build。

## 删「下一步」、MVP 讲完整

- 用户要求：去掉文末「下一步」段（Server Tools/MCP/Model Selection/Frontend Tools 四个前瞻列表），改为把 MVP 本身讲完整。
- 改动：把原「让 Agent 真正认识你的应用」并入快速入门为第 7、8 步（useCopilotReadable / useCopilotAction）；标题「快速入门：跑起来就能和模型对话」→「从零搭出完整 MVP」；结尾落点「MVP 已闭环：能聊天、能读状态、能改应用」。
- grep 确认「下一步」「## 让 Agent」已无残留。未跑 build。
