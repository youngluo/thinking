# 2026-08-06

## 新增文档：MCP 与工具调用

- 文件：`apps/docs/ai/Agent/MCP 与工具调用.md`（frontmatter: createdAt 2026-08-06 22:06, order 10, draft: true）。
- 主题澄清：用户原说「mac 和 工具调用」，经 brainstorm 确认实为 **MCP（Model Context Protocol）与工具调用生态**，非 macOS。
- 结构：术语澄清（function calling ≈ tool calling，三层差别）→ MCP 定位（Host/Client/Server）→ 协议机制（JSON-RPC + stdio/Streamable HTTP + 生命周期 + 能力三件套）→ 裸 function calling vs MCP 对比表 → 实战一裸 function calling 基线（OpenAI SDK）→ 实战二 MCP Server（@modelcontextprotocol/sdk，TS，get_weather + read_local_note）→ 生态与选型。
- 含 2 张 D2 图（架构、生命周期），均只用 group/subgroup 默认样式。
- 用户明确要求：先讲清概念，再分别给具体示例代码（故实战拆成两段对照）。
- 发布时需将 `draft` 改为 `false`。

## polish 优化（状态一 + 状态二）

- 状态一标题审查：去冒号、去序号前缀、去「裸」字、标题不加反引号；新增「function calling 的局限」独立章节（动机从引言拆出），术语澄清前置，「MCP 的定位」扩展为「MCP 的定位与架构」，协议节更名「MCP 的协议机制与传输层」命名对齐，两示例并行「X 示例」。
- 状态二正文重组：引言去掉目录预告；正文「裸 function calling」统一改为「function calling」；落实确认版标题树。
- 修正示例代码问题：基线补 `getWeather` 最小实现（示意）与 `if (!call)` 判空；MCP Server 加 `fetch` 的 `res.ok`/`current` 判空、注明 `wttr.in` 为示意、`read_local_note` 权限边界说明、顶层 await/ESM 运行提示、client 硬编码参数只为聚焦协议层的说明。
- 对比表新增「状态保持」行，修正「模型耦合」为「协议层与模型 API 字段解耦（Host 仍需支持工具调用的模型）」。
- 校验：`validate_markdown.py` 通过；`git diff --check` 干净（新文件无 diff）；修复 3 处列表标点（角色列表、传输列表非末项改分号）。全文无破折号、无「其他」误用。
