# 2026-08-07

## MCP 与工具调用文章重排（polish 流程收尾）

- 用户明确要求改为「MCP 先行、概念与代码交替」的对称结构，最终八节：
  - MCP 与工具调用（引言，MCP 先行导向句，删 USB-C 类比）
  - MCP 是什么（合并原「定位与架构」+「协议机制与传输层」，含两张 D2 图、能力三件套）
  - MCP Server 示例（server.ts / client.ts / 配置 / 权限与 ESM 说明，内容不变）
  - 工具调用是什么（合并术语三层 + 局限，删「结论先放在前面」套话）
  - 工具调用示例（baseline 代码，标题由「function calling 基线示例」去「裸/基线」统一为「工具调用示例」）
  - 工具调用与 MCP 的对比（对比表，开头补「工具调用此处特指未标准化 function calling 基线方式」说明）
  - 生态与选型（不变）
  - 小结（重写匹配新顺序）
- 标题约定：全文标题统一中文、去冒号、去序号、去「裸」、去「基线」，无中英文混跳。
- 交叉引用修正：MCP Server 节原「对比 function calling 基线示例」改为「对比原始 function calling」。
- 校验：`validate_markdown.py` 通过，`git diff --check` 干净（新文件无 diff）；全文无破折号、无「裸」、无「其他」、无失效前向引用。
- 文件：`apps/docs/ai/Agent/MCP 与工具调用.md`（仍 `draft: true`，发布时改 `false`）。

## Humanizer 去 AI 味 pass

- 按 `@skill:去AI味(Humanizer)` 扫描全文：无 em dash、无空洞升华、无空洞归因、无促销腔、无第一人称注入（严守用户「中性技术文档腔、不注入第一人称或幽默感」偏好）。
- 唯一 AI 痕迹：`工具调用是什么` 节三处装饰性加粗 `**模型层面…**`/`**抽象层面…**`/`**协议层面…**`，与用户「避免装饰性加粗」偏好冲突。改为 `term:explanation` 列表（模型层面/抽象层面/协议层面），契合用户偏好的明文列表格式。
- 顺带去掉「两者不是替代关系，而是…」的负向平行结构，改为「它们指向同一件事的不同抽象层级，谈不上替代」。
- 校验：`validate_markdown.py` 通过；grep 确认无 `**`、无 `——`、无「其他」。

## polish 整篇优化（状态一 + 状态二）

- 状态一标题审查：当前标题树满足全部约束（无数字/序号前缀、无反引号、无冒号、仅两层、同级抽象层级一致），且为前序多轮确认结果，无优化空间，直接进入状态二、不改标题。
- 状态二正文优化，四处实质修正（均非单纯换说法）：
  1. 引言删「本文先讲…再回头看…最后…」目录预告，改为直接动机（符合 skill「开头不复述目录」）。
  2. 第 43 行删「关键点是协议层与模型解耦」冗余重述（与第 13 行「不绑定任何具体模型」重复），保留换模型/跨 Host 两个具体收益。
  3. 第 191–192 行删「三件事…描述/发现/凭证」内联枚举（与后文四维度列表双重枚举且数量不符），改由列表独立承载。
  4. 小结删末句「上文两段可跑代码…」冗余指针，收紧为决策判断。
- 校验：`validate_markdown.py` 通过，`git diff --check` 干净；grep 确认无 `**`、无 `——`、无「其他」。
