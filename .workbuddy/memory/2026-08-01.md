# 2026-08-01

## 写了《提示词的组合、组装与传递》
- 文件：`apps/docs/ai/Agent/提示词的组合组装与传递.md`（order: 9, draft: true）。
- 定位：与《提示词工程》互补——后者讲「写什么/质量」，本文讲「组合-组装-传递的链路」。
- v1（11:11）：抽象视角，讲片段组合、每轮组装、跨 Agent 派生传递。
- v2（11:21）：按用户要求改为**以 Claude Code 为例的具体链路**——用户输入一句 → 捕获环境/记忆/工具 → 组装 system prompt → 拼装 messages（历史+本轮）→ API 请求（system+tools+messages 示意 JSON）→ tool_use 回注循环 → 回看最终上下文构成表。强调「那一句只占上下文不到 1%，是被追加而非全部」。
- 风格遵循项目约定：D2 链路图、JSON `fold` 示例、去 AI 味的简洁表格；对 Claude Code 内部细节用「通常/取决于版本」等留有余地的表述。
- 未运行 docs:build（项目约定除非用户明确要求才验证构建）。
- v3（11:24）整体优化：把 组合→组装→传递 作为主线，写进开头总览和 D2 节点标签（组合：上下文采集 / 组装：system prompt+messages / 传递：API+工具回注）；新增第 5 节 messages 组合 D2 图让「组装」可视化；把「设计启示」重构成按三阶段分组的决策规则，避免与第 8 节回看表重复。
- v4（11:38）按本地 polish skill（.agents/skills/polish）完成润色：状态一标题审查（去编号、去冒号、压到两级、管线章节统一为动词短语，主标题定为「提示词是怎么进入模型的」）→ 用户确认 → 状态二重写正文（设计启示三个 ### 压成加粗引导语、修复破折号、「其他→其它」）→ validate_markdown.py 通过、git diff --check 通过。文件 git 状态 untracked，未做任何 git 操作。
- 经验：polish skill 位于 `.agents/skills/polish`（项目级非标准位置），不在 Skill 工具可加载列表中，需手动读 SKILL.md 按其两状态流程执行；校验脚本 enforce：标题无序号/无反引号/无破折号、正文无破折号、统一「其它」、列表非末项分号末项句号。
- v5（15:04）按用户要求把主标题改为「从一句提示词到模型上下文」（在 4 个���选里选 1），校验通过。
- v6（15:05）文件名同步改为 `从一句提示词到模型上下文.md`（旧名无引用，安全 mv）；validate_markdown.py 通过。注意：rtk 会**自动暂存**工作区改动（git status 里旧路径显示 AD、新文件 untracked 属正常，未做任何提交）。
- v7（15:06）按 polish skill 查漏补缺：补引言适用边界句（不同 Agent 产品实现细节不同，但组合、组装、传递结构一致）；修正「越靠后的内容权重越高」为更准确的「模型通常对靠近末尾的内容更敏感」。校验通过。
- v8（15:12）把《从一句提示词到模型上下文》order 改为 3（紧跟《提示词工程》），并顺移中间 6 篇：缓存设计 3→4、RAG 4→5、LangChain 5→6、SSE 6→7、流处理 7→8、SDD 8→9；排序逻辑在 apps/docs/utils.ts（order 升序 → createdAt → 文本）。
- 2026-08-02（11:00）为文章新增「组装技能提示词」章节（插在「加载 CLAUDE.md」之后）：讲 Claude Code 技能两层加载机制（摘要常驻 system prompt 的技能目录 + 命中才加载完整 SKILL.md 正文），并与 CLAUDE.md 的常驻形成对照；同步配套：链路总览 C 节点与组合阶段描述补「技能」、system prompt 构成表加「技能目录」行、回看表加「技能正文」行。校验通过。
- 2026-08-02（11:12��用 polish skill 整篇优化第二遍：状态一标题审查通过（无需调整）；状态二改 5 处——技能对照表前补引导句「两层在上下文占用上的差别很大：」、「这里和 CLAUDE.md」→「技能与 CLAUDE.md」、system prompt 稳定性句补「技能目录」、JSON system 示意补「+ 技能目录」、总结补「命中技能时还会加载对应的正文」。校验通过。
- 2026-08-02（11:20）按用户要求改章节标题：「那一句最终去了哪」→「回看上下文构成」（去掉口语问句与「那一句」指代，动词短语与元章节风格一致）。校验通过。
- 2026-08-02（11:22）polish skill 整篇优化第三遍：状态一通过；状态二仅 2 处——技能对照段「在命中时」→「命中后」（减少「命中」重复）、回看表「技能正文」行的来源列由「命中时按需加载」改为「SKILL.md 正文」（原来源列与末列重复说同一意思）。校验通过。文章已收敛。
- 2026-08-02（11:27）polish skill 第四遍：状态一通过；状态二仅 1 处——总结里「项目记忆」→「CLAUDE.md 记忆」（全文唯一一次使用「项目记忆」，与其它处不一致）。校验通过。四遍优化后文章已无实质问题。
- 2026-08-02（11:37）polish skill 第五遍：核验两疑点——`when_to_use` 字段经官方文档确认属实（description+when_to_use 合并截断 1536 字符，原文表述准确，无需改）；**发现第 172 行「命」字字节损坏**（E5 91 BD → 3× U+FFFD 替换字符），用 python 字节级修复还原为「仅命中时」，全文件 U+FFFD 归零。校验通过。教训：polish 校验脚本不查 U+FFFD 损坏，需 grep/hexdump 专项扫描；多次润色后仍可能潜伏编码损坏。
